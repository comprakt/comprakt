use crate::graph::*;

impl Program<'_> {
    pub(crate) fn register_allocation(&mut self) {
        // SSA-based register allocation
        // => SSA deconstruction on the go
        unimplemented!()
    }
}
